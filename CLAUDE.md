# CLAUDE.md

Este proyecto usa **`AGENTS.md`** como fuente de verdad para los agentes. Léelo primero; aplica a Claude Code igual que al resto.

@AGENTS.md

---

## Notas específicas para Claude Code

- Antes de implementar, abre `docs/tasks.md` y trabaja la primera tarea sin marcar.
- Respeta las "Reglas de oro" de `AGENTS.md` (lógica en `src/core/`, autorización en el servidor, una sola fuente de verdad por proyecto).
- Ejecuta `npm run typecheck` y `npm run test` antes de dar una tarea por terminada.
